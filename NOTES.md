# Notes — known limitations and what's not done

**Updated 2026-09-15 14:30.** Bump this stamp with every edit to this file.

Honest state of the kit **right now**: what is missing, what is weak, and what is open.
Everything here is deliberate — either deferred with a reason, or a weak point worth
naming rather than discovering.

It does not hold history (that is [`DEVLOG.md`](DEVLOG.md)), what the runs found
([`RUNS.md`](RUNS.md)), or the rules for working on this repo (`CLAUDE.md`). If a fact
here stops being true, it belongs in one of those and not both.

## Known limitations

**A rule renders as one markdown bullet.** `harness.rules/markdown` emits
`- **Title** text`, so a rule's `:text` supports inline formatting (`code`, *emphasis*) and
nothing else — no fenced code blocks, tables or sub-bullets. A rule needing those has no
home today. The fix is to extend the renderer; the tempting workaround is the trap below.
Documented at all three points where you'd hit it: the rule-source header, the renderer's
docstring, and `harness-seed/README.md`.

**Nothing detects a rule written outside the markers.** This is the weakest link in the
rule-source design. A rule hand-written into `AGENTS.md` outside `<!-- agent-rules:begin -->`
survives every `bb rules-sync`, so the drift gate never complains — and it reaches only
clients that read that file, which by the independence rule excludes the agent verifying the
Coder. Generating one mirror rather than several keeps this to a single file, but does not
fix it; `harness-seed/CLAUDE.md` is a hand-written stub and is a second place it could
happen. Silent, and exactly the failure the design exists to prevent. It is documented in
four places and enforced in none. A check would have to heuristically flag bullet lists
outside the block, which risks false positives on legitimate hand-written content; that
tradeoff hasn't been made yet.

**No CI.** `bb gates` runs locally only. There is no Actions workflow, so nothing verifies
the gates on push or on a pull request.

**A `~$` cost is an estimate.** A direct Anthropic dispatch has no cost in its response, so
the report computes one from usage × the list prices in the profile's `:pricing` — base
input, output, cache writes and cache reads, nothing else: no batch, data-residency or fast-mode
multipliers, no negotiated rate. The prices are as of the profile's `:as-of` date and nothing
checks that date against the pricing page; `bb profile` checks only that the date and source
are there.

**Only one kind of documented claim is gated.** `bb report-check` re-renders every report
published in `RUNS.md` from `runs/` and fails on any difference, in both directions — a
published table with no record, and a record nothing publishes. That covers the one claim in
this repository backed by committed data. Every other assertion a document makes is checked
by someone reading it.

Running the fenced commands in the documents is the obvious second gate and a much weaker
one. It would **not** have caught the failure that prompted the first: `.local/README.md`
offered a re-rendering command as proof the figures were auditable, and that command worked —
it named `d6`, one of the six records that render. What was false was the claim around it,
which generalised past the single case it had been tested on. A command runner catches path
rot and renamed tasks; it does not catch a true example supporting an untrue sentence.

## Not built, deliberately

`harness-seed/README.md` §"Deliberately left out" is the authoritative list — the
orchestration loop, triage, per-gate timeout — each with the reason and the order to add
them back. `method.md` §12 argues why you should not build the loop
speculatively in the first place.

## Harness portability

`portability.md` is the standing analysis, and the only place these facts live: which
interactive seat, which model per role, what each candidate client actually offers, and the
dispatch design that follows.

The distinction it turns on: **a project has one interactive seat, one dispatch harness, and a
different model family per dispatched role.** §05's independence rule lives in the harness, by
family — not in the seat.

**Both halves are built.** The seat half, and of the dispatch half: provisioning, the
Blueprint slice as loadable source, the `:deps-sigs` precondition, profiles, the run report,
and the API-backed runner — two request adapters, a tool loop, and the two-call provenance
that turns a completion id into the provider that actually answered and what it cost.
`resources/profiles/` is dispatched from, not merely checked.

What remains deliberately unbuilt is the ORCHESTRATION around it: the loop itself, triage
with a capped retry, and the append-only run log. Triage has been done by hand every time.
The driver is no longer throwaway: after D7, D8 and D9 ran on one script carried forward,
it was committed as `harness-seed/dev/run_loop.clj` (`bb run-loop`). It is the loop's
steps as commands, with a human routing every failure between them, which is not the
orchestration loop.

**Readiness criterion 6 is closed twenty-four times over.** §03 step 5 — the whole loop, end to
end — has been run against `sandbox/` twenty-four times, over twenty-three task ids: five through
`ManualRunner` with the roles played by hand, then nineteen dispatched to real models for
about $2.93 (D3 dispatched the Coder only; only D18–D21's Coders are priced). D19–D21 are the
three tasks of a stage, S1, run from a `stage/s1` branch and each merged onto it.
**[`RUNS.md`](RUNS.md) is the record, and a run is not finished until it is in there.**
Its report tables re-render from [`runs/`](runs/), which is committed so the
figures are checkable. The driver is committed; each run's working directory is not.
Every run found something, and most of what `harness-seed/` has grown since exists because a
run made it necessary; the design conclusions are in `portability.md`.

### The register: what the runs found, and where each stands

Most of the runs' findings were fixed where they were found and are explained in
[`RUNS.md`](RUNS.md). This table holds the ones that outlived their run: what was found, where it
stands, and what was done about it. A **Status** of *open* means nothing is built; *fixed* names
the commit; *watch* means built or a habit, and watched for recurrence; *obsolete* means the question
no longer applies, with the reason. The rows are not
duplicated in `RUNS.md` — a second copy of a list is the drift these runs kept catching in the code.

| # | Finding | From | Status | What was done, and what remains |
|---|---|---|---|---|
| 1 | **Nothing tells a task that rewrites a namespace what else depends on it.** Run 5 rewrote `sandbox.render` by hand, and its dependents stayed green by luck. D12, the first dispatched rewrite, broke `property_test.clj`'s round-trip properties, in a file no role owned, and no role mentioned the dependents. The whole-suite test gate caught it, and an Architect amendment fixed it on retry. So the safety net holds, but nobody learns of a dependent before breaking it. | Run 5, D12–D14 | **partly fixed** `4e6c4eb` — dependents shown, the run pauses on a note; `0ecb64d` — the mechanical routings proposed and recorded; the remainder **watch** (2026-09-15) — the judgement routings stay a person's call, by decision | **Built after D12 (2026-09-14).** `sigs/dependents` reads the files that require a task's implementation namespaces from clj-kondo's analysis, and `bb run-loop start` adds them to every packet's `:files/context`. For `sandbox.render` that is exactly `api.clj` and `property_test.clj`; a new namespace gets none. The Coder is told they must keep working, and the Reviewer to check them. **D13 tested it with a model, and visibility was not enough.** A controlled rerun of D12's first attempt: the dependents were in every packet, and the Coder had also read the original `render.clj`, whose docstring explains round-trip. It still implemented the conflicting contract with no note, and the test gate went red in `property_test.clj` again. The remaining gap is specific: nothing tells a role what to do when the contract contradicts a file it was shown. The `note` tool's triggers do not include it, and the Coder is told dependents "must keep working" without being told which wins. **D14, D13 with only the Coder switched to Claude Fable 5.1, closed that half for this Coder.** It reported what `parse` throws on parenthesised input, noted the conflict, naming both failing properties and the two ways out, and followed the contract as it said it would. What is still open is that **the note stops nothing**: nothing reads notes mid-run, so the Tester was dispatched and the gates ran into the failure the note had predicted. **`bb run-loop` now pauses on notes** (2026-09-14): a dispatch in `start`'s sequence that leaves a note stops the run before the next dispatch, and waits for `continue`, or for `amend` and `retry`. On D14's recorded Coder result it would have stopped before the Tester. **D15 verified it live:** the run paused on the Coder's note, the Architect amended from the note alone, and `continue` finished with one green gate run and no red one, where D12, reaching the same final contract, spent two Tester dispatches and a red gate. What stays open is that the decision at the pause is still a human's. **The mechanical half of that decision is the driver's now (2026-09-14).** Nine triage decisions were on record by D17 (D7, D8, D12, D15, D17; `bb` over `runs/*.edn`); three routed by rule — a red gate in a file no role owns, a note at the pause — and the rest by judgement over a Reviewer's or triage's finding. `bb run-loop check` now records a `:routing` event on every red gate: the impl, test and dependent files the gate's output names (by path, by file name, or as the namespace a failing clojure.test block ran under, since a whole-suite gate names every namespace and a failure's own line names the throw site), the owner that implies (`:coder` when only impl files, and when only the task's own test files on the test gate — method §07's default, row 17; `:tester` when only test files that did not compile or failed format or lint; none for a dependent or a mix), which role a retry would go to and with which sources. Nothing is dispatched on it; the human's `:triage` event follows it in the record, so the two can be compared. Over the recorded red gates (`.local/runs/triage/retro.bb`): D13, D14 and D17 name the dependents `api.clj` and `property_test.clj`, no owner, retry the Coder with `:architect` and `:gate` — D12's decision, and D17's; D12 itself names nothing, because its record predates the dependents list the proposal reads. What stays open: the judgement routings. **D20 was the first red gate since it was built:** the proposal named the test file and the Tester with sources `[:gate]`; the decision was the Tester with `[:architect :gate]`, agreeing on the role for a reason the rule cannot see (row 17). **The remainder is decided (2026-09-15): the judgement routings stay a person's call, and nothing more is built for them.** The record settles what that judgement is. Seven triggers led to a retry or a `continue` on a decision (`bb .local/runs/triage/judgement.bb`, over `runs/*.edn`): five by judgement — the Reviewer's finding (D7), a hand contract probe (D8), a Coder's note at the pause (D15, D17, D19) — and two by the red-gate rule (D12, D20). At five of the seven the Architect amended the spec before anyone was re-dispatched (D7, D12, D15, D17, D20); the two that did not were D8, whose defect was the Coder's and was found from outside the loop, and D19, whose note was an observation and was continued. So the question asked at every one of these points was *is the contract at fault?*, and it usually was. No gate output, note or finding carries that answer: the person reads the sentence against what two roles did with it (D20: both readings of target 8 were allowed by the wording). The mechanical help that fits is already built — the pause stops the run before the damage (D15 against D12), `leaks` shields the Tester from a Reviewer's text (D7's hand shielding is its `:reviewer-source` rule), and the red-gate proposal names the files and the role — and no rule over these seven would have changed a decision. Watch for two things: a judgement routing decided wrongly in a way a rule would have caught, which reopens this; or the amend rate holding over more tasks, which is not a routing finding at all but a Blueprint one, and says the judgement is better spent on the spec's sentences before dispatch (S1's Step 2) than on routing after. *(This row used to name `api_test.clj` as a dependent; it does not reference `render` or `canonical`.)* |
| 2 | **Whether D4's Tester failure was tier or family was never established.** D5 swapped `deepseek-v4-flash` for `gpt-5.2-codex` and pinned the provider in one change, and D6 then showed the real cause was a missing stop condition in the prompt. The profile change was right; the experiment that would separate the variables was never run. | D4, D6 | **obsolete** (2026-09-15) — not to be run, by the user's decision | Nothing was built, and the experiment is not going to be run. It would cost one run of the fixed prompt on the model D4 used, to answer a question about a model the kit no longer dispatches: the Tester has been an OpenAI model since D5 and is `gpt-5.6-sol` on the standard endpoint now. The profile change was right either way, and D6 settled what the run itself needed to know. The confound stands on record as a lesson, not a question: D5 changed two variables in one step, and a run that swaps a model should change nothing else. |
| 3 | **A test named *at any depth* or *recursive*, satisfied by a fixture one level deep.** Caught by mutation in run 4 and again in run 5, in different namespaces by different authors. No gate can see it; it is a habit for review to watch for. | Runs 4, 5 | **watch** | Nothing to build; a habit for review. |
| 5 | **Nothing decides how much of a finding the Tester may see.** A Reviewer's findings quote the implementation, and handing them to the Tester is the leak provisioning prevents. D7's triage passed the Reviewer's note instead, by hand — and the note still named `expr/literal`. Triage, once built, needs this as a rule. | D7, D8, D12 | **fixed** `0ecb64d` (2026-09-14) | **Built 2026-09-14.** Every Tester retry on record (D7, D8, D12) had been shielded by hand; `bb run-loop retry tester` now refuses feedback that carries the implementation, on four rules: `:feedback/from :reviewer` at all (the Reviewer's text is derived from the diff it read); an `:files/impl` path or file name; a var the impl defines that the slice's `:interfaces` did not grant, as a whole symbol (`sigs/impl-names`, from clj-kondo's definitions); a fenced code block. `--allow-leak` sends it anyway, and either way the `:triage` event records the leaks found and whether they were allowed, before the dispatch. Run over every recorded retry's feedback (`.local/runs/triage/retro.bb`): D8's and D12's Tester feedback is clean, as the hand shield intended; D7's is refused on the Reviewer source alone — the judgement that passed its note becomes `--allow-leak`, on record — and D7's Reviewer findings, which the Coder got, would be refused on the source and the code block. What it does not see: `expr/literal` in D7's note is a context var, not an impl var, so the "mild leak" the triage recorded passes; and a rendered value in gate output (D12 withheld `"(((0"` from the Tester) is not a name. A Coder's retry is not checked. D20's two Tester retries were its first live runs, and neither feedback had a leak. |
| 6 | **Property targets that describe only shape.** D7's three did, and a fold computing the wrong number satisfied every one of them and a Tester that followed them faithfully. No gate can see a contract that asks for too little; it is a habit for the Architect. | D7 | **watch** | Nothing to build; a habit for the Architect. |
| 7 | **The Tester's retries run to the iteration cap.** D6's second retry used 24 of 24; D7's was capped at 24 with no final message, having already written a file that passed. Not the `note` tool: it made no note calls. A capped dispatch reports `:done` by design — `harness.agent` leaves the decision to its caller — so only `:capped?` in the run's events says so. | D6, D7 | **watch** | Nothing built; only `:capped?` in the run's events says so. |
| 8 | **A dispatch that produced nothing cannot be diagnosed afterwards.** `AgentResult` carried no transcript, so B1's `glm-5.3` — 24 completions, 25 tool calls, no file, no note, no final message — left nothing to say *why*, and D3 and D6 were diagnosed only by paying to re-dispatch. | B1, D16, D17 | **fixed** `d784b71` (2026-09-14) | **Built 2026-09-14.** `converse!` keeps `:turns` (each completion's text and tool calls, with arguments and results), `outcome` carries them as `:runner/meta :transcript`, and `bb run-loop` records a copy on the dispatch event with every string capped at 500 characters, the whole one in `<step>.transcript.edn`. D16 is the first record with them: 4–6 KB per dispatch. D17's showed the order of a Coder's work — `parse` run in the REPL two turns before the conflict note — which no earlier record could. What stays open: B1, D3 and D6 are still blank, and a capped transcript shows the shape of a dispatch, not its content — the full one is gitignored and lasts only as long as `.local/`. |
| 9 | **Nothing retries an upstream rate limit.** The Tester was switched to `gpt-5.6-sol` on `openai/flex`, and flex refused 3 of 4 requests inside 75s and 4 of 5 dispatches in a row. So the profile moved to OpenAI's standard endpoint, which answered every request. That settles the Tester, not the gap: a 429 is now reported as a failed dispatch rather than an empty one, but a capped retry belongs to the deliberately unbuilt triage, so any role whose endpoint is limited simply fails, with `:allow_fallbacks false` on every OpenRouter role. | switch | **fixed** `ee5e869` (2026-09-14) | **Built 2026-09-14.** `harness.agent/post!` sends a request again on a transient error — 429, 500, 502, 503, 529, or no connection (`adapter/transient?`) — up to four attempts in all, waiting the host's `Retry-After` when it sends one and otherwise 2s × the attempts so far; a 400, a 401, a refusal or a truncation is not sent again. How many times a dispatch was sent again is `:retries` in its meta and on the dispatch event when non-zero; a run that needed none records nothing new. Tested against a stub that answers 429 then 200, one that never stops, a 400, and a `Retry-After: 0`. Not yet exercised by a live rate limit: the Tester left flex before this existed. Still no *triage*-level retry — a dispatch that fails after four tries fails. **A refusal fails the same way** (2026-09-14): with the Coder on Claude Fable 5.1 and no fallback model configured, a `stop_reason` of `refusal` is a failed dispatch naming its category. Its first live occurrence was a smoke probe worded as "decoy files, only one holds the answer", which the cyber classifier refused; plain wording and a real Coder dispatch with the full rules prompt were not refused. Whether refusals come up in real tasks is unmeasured. |
| 10 | **`provenance_test/the-id-goes-in-the-query-and-the-key-in-the-header` failed once, unexplained.** All four of its assertions failed in one `bb gates` run on 2026-09-14, then passed in all eleven runs since. The full output was not captured, so the cause is not established. One suspect is today's change to fetch generation records in futures: a fetch left polling a stopped test server could reach a later stub that reuses its port. That is unconfirmed, and `settle` derefs on every exit of `converse!`, which argues against it. Capture the output the next time it fails. | 2026-09-14 | **watch** | Nothing; capture the output the next time it fails. |
| 11 | **Nothing checked that an implementation calls only its `:deps-sigs`.** D8's Coder called `expr/literal?`, which its slice omitted (the Architect's omission), and nothing noticed. | D8, D18 | **fixed** `1bd4cd1` (2026-09-14) | **Built 2026-09-14.** `sigs/undeclared-calls` reads clj-kondo's var usages from the impl and flags every call into a context namespace that no `:deps-sigs` entry names, or whose arity the entry's argv rejects; `bb run-loop check` runs it as the `:calls` step after gate 0 and stops before the shell gates on a hit, with the calls as gate feedback. Run over D7–D17's recorded implementations it flags exactly D8's `expr/literal?` and nothing else. Recovery when the slice was wrong is `amend` then `check`, no dispatch. D18 ran it live (242ms, clean); no live run has hit it yet, and it sees only calls kondo resolves — a var reached through `resolve` or a macro it cannot expand is not a call to it. |
| 12 | **Two independent sources make sandbox traversals quadratic, and only one is a task's to fix.** (1) Calling `check!` at every recursive call re-validates each subtree. A task can remove that: D10's `measure` went from 3,780ms to 10ms at 800 nodes with `check!` once at the top. (2) `sandbox.expr/binary` validates the whole subtree it is given, so any rebuild through the constructor is quadratic, and no task can change that: D8's `bind` with `check!` once still took 3,692ms, half of its 7,351. D7's Reviewer flagged source 1; D8's and D10's, given the same shape, did not. | D7–D15 | source 1 **fixed** by the rule `4e6c4eb` · source 2 **accepted** | Source 1 now has a Coder rule: `:shapes-are-the-contract` says to validate once, at the entry, and recurse through a helper (2026-09-14). D11's Coder followed it, validating once and recursing through a private helper, and cited the rule; `paths-to` measured linear, 11.6ms at 800 nodes. D12's Coder did the same on both attempts, and `render` measured 9.9ms at 800. D13, D14 and D15 validated once as well, but they rewrote the same `render` as D12, so that is five runs over two tasks, not five independent samples: supported, not established. Source 2 is **accepted for now** (2026-09-14): validation at construction stays, its cost is measured and documented here, and it is revisited if a run needs rebuilds to be linear. The evidence is in `runs/d9.edn` and `runs/d10.edn`. *(This row said "no task can fix it" after D9; D10 showed there were two sources.)* |
| 13 | **A test that cannot fail.** D12's `implementation-does-not-alias-parser` filters namespace names with `(namespace sym)`, which is `nil` for a name like `sandbox.parse`, so it passes whatever `render` requires. Only mutation showed it; gate 4 happened to enforce the same target. D8's single invalid-input fixture was the same kind of thing, rejected by a constructor before the check it claimed to test. Mutation is the only thing that finds these, and it runs by hand after the loop. | D8, D12 | **watch** | Nothing built; mutation is the only thing that finds these, and it runs by hand after the loop. |
| 14 | **A run's record does not hold the bytes its gates passed.** `record!` copies each role's files from that role's own worktree into `final/` and `:run/files`, but gate 0 repairs and formats in the gate worktree, where the gates and the Reviewer's diff run. D19's gate 0 re-indented two lines of the Tester's file, so `final/` failed the stage's format gate when merged; nothing in the record says gate 0 changed anything. Five committed records — D9–D13 — hold a file `cljfmt` rejects although their format gate passed (the command is in `RUNS.md` §D19). `final/` also flattens paths to `final/<file-name>`, so a merge re-derives each path from the spec, and two files of one name would overwrite each other. | D19 | **fixed** `a514b0e` (2026-09-15) | **D20 made it worse than formatting:** the Tester's file had an unmatched `]` at line 99, gate 0's delimiter repair removed it, and the gates ran the repaired file. **Fixed after stage S1** (the driver was held unchanged through it): `record!` copies from the gate worktree whenever that copy is current — a gate run after every Coder and Tester dispatch (`gated-current?`) — into `final/<path>`, and keeps each file gate 0 changed as the role wrote it in `final/as-written/<path>`, recorded as `:run/files-as-written`; `check!` records a `:gate-0` event with the hunks of each file it changed, and nothing when it changed none; `final-files` reads by path and still reads a flat `final/`. D7–D18 replay byte for byte. **D19–D21 re-recorded** from the gate worktrees' bytes copied before teardown (`.local/runs/row14/rerecord.bb`): D20 and D21 byte for byte, D19's `:run/files` now the gated Tester file (which passes `cljfmt`) with the written one in `:run/files-as-written`. Five tests; all thirteen exact-match mutants killed (`.local/runs/row14/mutate.bb`). What remains: D9–D13's records keep the written bytes, their gate worktrees gone; D19's record has no `:gate-0` event, its state predating it; D20's attempt-1 repair was never a final file and is only in `RUNS.md`. |
| 15 | **A decision at a pause that changes nothing leaves no reason in the record.** `continue` writes `:continued {:after :next}` and nothing else, where a retry's triage decision is written before its dispatch. D19 paused on a Coder note that was an observation, not a conflict, and was continued without an amendment; the reason exists only in a local file. | D19 | **fixed** `3839db5` (2026-09-15) | `continue <run-dir> [<decision.edn>]`: when no `amend` or `retry` was recorded after the last pause (`decided-since-pause?`), `continue` refuses without a decision file `{:decision "..."}`, and records the decision on the `:continued` event; a blank one is refused too. After an `amend` or a `retry` the event is as it was, so D15's and D17's records replay unchanged, and D7–D18 replay byte for byte. The pause message names the file. Tests extended in one deftest; six mutants, five killed at first — the survivor dropped the decision file in `-main`, which nothing tested, and a test of the command line then killed it (`.local/runs/row15/mutate.bb`). What remains: D19's record keeps its reasonless `:continued` — its reason is in `RUNS.md` §D19. |
| 16 | **A failed evaluation reached the model as an empty success.** `clj-nrepl-eval` exits 0 when the evaluated code fails — a throw, a file that will not read, a symbol that will not resolve — and prints the error on stderr; `nrepl_eval` returned stdout alone on exit 0. D20's Tester called the stub it is given, which throws by design, saw three blank results, and stopped under `:no-repl-no-edits` with the nREPL alive; its first attempt never saw that its own test file did not read, and took `=> :loaded` after a failed `require` for success. Both established by replaying the calls through `harness.tools/invoke` against the same nREPL. Blank results also in D17's and D18's transcripts. **And the non-zero branch was broken:** a local `err` shadowed the `err` helper, so an unreachable nREPL came back as "String cannot be cast to IFn". | D20 | **fixed** `7a28882` (2026-09-15) | `tools/eval-result`: stderr always returned; a result is flagged only for a stderr line beginning with one of the five prefixes `clojure.main/ex-str` starts its eight error phases with (read from Clojure 1.12.5's source), since warnings go to stderr on success; a failed form, a timeout (on stdout, exit 0) and an unreachable nREPL are worded apart, and a failed form says the nREPL answered, so the rule that stops on a dead REPL cannot read it as one; stderr comes first on a failure and both result paths are clipped. Nine tests, from probed output and the source's formats; all seventeen exact-match mutants killed (`.local/runs/row16/mutate.bb`). Checked live on D20's Tester nREPL. The flag reaches the model only as the `ERROR:` prefix — neither adapter sends `:error?`. What remains: the Tester's prompt still does not say the namespace under test is a throwing stub (the stub's own message now does); an unreachable nREPL's stderr carries `clj-nrepl-eval`'s stack trace with absolute local paths, which a committed transcript would keep. First read live by D21's Tester, which saw the stub's error and carried on. |
| 17 | **The routing proposal's Tester rule contradicts method §07 for the test gate.** `propose-routing` makes `:tester` the owner when only test files are named. An assertion failure in the task's own test file always names only that file, so every ordinary red test is proposed as the Tester's, where §07 says the default owner of a failing test is the Coder. D20's first live proposal was right, for a reason it cannot see (the test mis-encoded an ambiguous target); over a real implementation bug it would say the same. | D20 | **fixed** `2b984a4` (2026-09-15) | The rule came from D7–D17's hand decisions, none of which was an assertion failure in the task's own test. `propose-routing` now makes the Coder the owner when only the task's own test files are named on the test gate, with advice naming §07's default and the triage decision the other way — `amend`, then `retry tester`; the Tester stays the owner when such a file did not compile (`Syntax error`) or failed format or lint. Over every recorded red gate (`.local/runs/row17/retro.bb`): D12–D17's proposals are unchanged, and D20's two would now say the Coder beside a decision for the Tester — the disagreement the record exists to show; the proposals already in `runs/d20.edn` stay as recorded. Tests changed and added in one deftest; all four exact-match mutants killed (`.local/runs/row17/mutate.bb`). What remains: a compile error the output does not call `Syntax error`, and an assertion that fails because the test is wrong, both still need the human. |
| 18 | **A dispatch that wrote nothing reports the previous attempt's file.** `:files` comes from git status, so D20's tester-r1 — whose final message says "No file was written" — printed `files=["test/sandbox/constants_test.clj"]`, attempt 1's untracked file, with status `:done`; `check` was then run over an unchanged file. | D20 | **fixed** `8ae2a42` (2026-09-15) | `api-runner` takes a `worktree-snapshot` of what git lists as changed or untracked before the dispatch, and `:files` is `written-since` it — the paths new or different when the dispatch ends. A retry that writes nothing reports nothing; one that changes the file reports it. And `bb run-loop` now prints **WROTE NOTHING** with the start of the final message whenever a Coder or Tester dispatch finishes having changed no file (`wrote-nothing?`), so the slip D20 recorded is harder to repeat. Three tests; all five exact-match mutants killed, the first restoring the git-status rule (`.local/runs/row18/mutate.bb`). What remains: a dispatch that rewrites a file with identical bytes reports nothing, which is what it changed. |
| 19 | **`bb report-check` misreads a labelled fence.** `published-reports` takes a block to be three backticks and a newline, then anything up to the next three backticks. A labelled opening fence (three backticks then `sh`) does not match, so its closing fence opens the next block and every block after it pairs wrong. §D19's command block did that, and D20's table, the next one published, was reported as missing. That failure was loud because D20's record went unmatched; a table with no record, after such a fence, would pass unnoticed. | D20 write-up | **fixed** `399addc` (2026-09-15) | A fence now has to start its line, opening and closing, and its label is captured: labelled blocks are matched and consumed whole, and only unlabelled ones can publish a report. `RUNS.md` parses to the same 26 reports, byte for byte, as under the old pattern (compared as data before the change), and §D19's command block has its `sh` label back with `bb report-check` in sync. One test for a labelled fence before a report, a recordless table after one (now `:no-record`), a labelled block that looks like a report, and backticks inside a line, each checked as drift against the record. Three mutants: two died at once; the third, a closing fence that need not start its line, survived because a mis-paired block swallowed prose naming the run and still yielded its key — the assertions then checked drift, a line ending in backticks got a case, and it died (`.local/runs/row19/mutate.bb`). What remains: a fence indented inside a list item is not seen, and no report in `RUNS.md` is written that way. |
| 20 | **`canonical`'s output is tested by nothing.** D21's mutant making `sandbox.api/canonical` return its input survived the whole suite: `property_test.clj` checks only that it is idempotent, which returning the input satisfies, and `api_test.clj` does not call it. The S1 Blueprint left `canonical` to the dependents of `api.clj`, and they protect less of it than it assumed. | D21 | **fixed** `1d46abc` (2026-09-15) | **Fixed on `experiments/d7-d15` (2026-09-15).** `api_test.clj` pins the normal form on examples — padded, tabbed and newlined input comes back single-spaced, a lone operand trimmed, canonical input unchanged — and `property_test.clj` states what the form *is*: over any expression with every gap replaced by one to four spaces, tabs or newlines, `canonical` equals the tokens joined by single spaces. D21's survivor, `canonical` returning its input, now dies (`bb .local/runs/d21/mutate.bb sandbox src/sandbox/api.clj .local/runs/row20/mutants.edn …`: 2 of 2 killed, the second a trim-only `canonical` that keeps inner spacing). The sandbox fixture is one deftest and one defspec bigger than the stage found it, which every later run starts from. |

**Row 4 left the table on 2026-09-14, after D8**, and it was the most expensive open
row. Property targets reached only the Tester, which cost D7 an unannounced amendment
and D8 a Coder retry and a blind Reviewer. They are on every packet now, and a contract
change can be announced as `:architect`. `DEVLOG.md` has the change.

**Two entries left this table after D7, because a run answered them.** Nothing had called
`packet/for-retry`, and no model had been offered the `note` tool. They were two halves of
one circuit: a role reports something outbound with `note`, and `for-retry` carries it — and
a failed gate's output, or a Reviewer's finding — back in as `:task/feedback`. D7 closed the
circuit with real models: the `note` tool was offered to six dispatches and used in three,
and both roles converged on a retry that carried feedback. [`RUNS.md`](RUNS.md) §D7 has the
detail; rows 4 and 5 above are what exercising it found.

The framing that made the circuit tractable still holds: **the channel was never missing, it
was prose.** Run 4's Coder did say the contract was silent about dividing 7 by 2, and D3's
did say why it declined a `:deps-sigs` entry — both into `:stdout`, which nothing reads. In
D7 the same observation, about the same division, arrived as a note.

## Code review of `experiments/d7-d15`

**2026-09-14, `/code-review high` against `main`, checkpoint commit `4e6c4eb`.** Four bugs,
all in code this branch added, and none covered by a test. Each was checked against the code
before it was triaged. The review found nothing wrong in the adapter's error, refusal and
`max_tokens` handling, the background generation-record fetches, `provision/review-diff`, the
report's step column and tier cell, or property targets on every packet. **Numbered R1–R4 so
they cannot be confused with the run rows above.** One caveat on independence: the reviewer is
the same model family that wrote the code (method §05), so a clean area is weaker evidence
than it looks.

**Triage: all four were *fix now*; none was *no change*.** Each is a real defect, reachable
through the documented use of `bb run-loop` or `sigs/dependents`, with a small fix and a test
that would have caught it. None is a false positive, and none is harmless at the harness's
intended use. **All four were fixed on `experiments/d7-d15` the same day, one commit each.**
Each fix has a regression test, and every mutant that reverts or weakens it fails a test. R4's
recovery path was also checked live against real git. The table below is kept as the record of
what was wrong; the section can move to `DEVLOG.md` at the stable point.

| # | Finding | Where | Severity | Decision | Fix, and the test that proves it |
|---|---|---|---|---|---|
| R1 | **A valid spec can be refused before dispatch.** `start` appends dependents to `:files/context`, then runs `sigs/violations` over the enlarged list. `sigs/definitions` maps clj-kondo's filenames back to paths by *bare file name*, so a dependent `src/a/web/core.clj` shadows a context file `src/a/util/core.clj`. A correct `:deps-sigs` entry for `a.util.core` is then reported `:unknown-namespace`. The reviewer reproduced it. The name-matching flaw predates the branch; the dependents change now triggers it with nothing the Architect wrote, and `core.clj` in several directories is common in Clojure. | `harness-seed/src/harness/sigs.clj` (`definitions`), reached from `dev/run_loop.clj` `start!` | medium | **fixed** `b2be718` | Match analysis filenames to paths by normalised relative path, not file name. Test: two context files named `core.clj` in different directories, both namespaces' signatures check out; plus the same through `with-dependents`. |
| R2 | **The Tester's own target can be listed as a dependent.** `dependents` excludes `:files/impl` but not `:files/test`. When a rewrite already has a test file requiring the namespace, that file joins every packet's context: the Coder is told it "must keep working", the Reviewer to check it, and the Tester sees its own target as read-only context. A careful Coder notes the expected breakage, and pause-on-notes stops the run for nothing. Not hit so far: `sandbox.render` has no test file of its own. | `harness-seed/src/harness/sigs.clj` (`dependents`), `dev/run_loop.clj` `start!` | low | **fixed** `bb1bd7a` | Exclude `:files/test` as well as `:files/impl` (pass both from `start!`). Test: an existing `test/app/render_test.clj` requiring `app.render` is not a dependent of a task whose `:files/test` names it. |
| R3 | **The Tester's stub goes stale after an amendment.** `stub/write!` runs once, in `start!`, from the original slice. The documented pause flow is `amend` then `retry`/`continue`. `amend!` records `:slice-changed?` but never rewrites the stub, and neither `continue!` nor `retry tester` does. An amendment that adds an interface or changes an arity leaves the Tester prototyping against a stub that contradicts its packet. D15's amendment changed only `:deps-sigs`, so it did not hit this. | `harness-seed/dev/run_loop.clj` (`amend!`, `continue!`, `retry!`) | low–medium | **fixed** `353b983` | When the slice changed, regenerate the stub in the Tester's worktree and record it in `:harness/wrote`, in `amend!` or before the Tester's next dispatch. Test, with dispatch stubbed: after an amendment adding an interface, the stub defines it. |
| R4 | **A failed `start` can be neither cleaned up nor restarted.** `start!` writes `state.edn` before provisioning but saves `:sessions` only after all three worktrees exist. If a later provision throws, earlier worktrees and nREPLs are orphaned, and `teardown` knows none of them. If the signature precondition fails, the message says "fix the spec, then teardown", yet `start` still refuses because `state.edn` exists, and the kept branches make `git worktree add -b` fail. Recovery is deleting `state.edn` and the branches by hand. | `harness-seed/dev/run_loop.clj` (`start!`, `teardown!`) | low–medium | **fixed** `864ccbd` | Persist each session as soon as it is provisioned. Make the refused-start path say exactly what to remove, or let `teardown` also delete the task branches and `state.edn` when no dispatch happened. Test, with provisioning stubbed to fail on the third worktree: the first two are recorded and torn down. |

## Adjacent work

The kit was extracted from a private harness, and several defects found during extraction
still exist upstream — `harness-seed/PROVENANCE.md` lists every divergence with its reason,
and doubles as the fix list for that project. Back-porting them is pending.

